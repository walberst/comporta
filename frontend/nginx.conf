server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota desconhecida cai no index.html e o Vue Router
    # resolve client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
